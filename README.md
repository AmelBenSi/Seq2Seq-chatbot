# Seq2Seq chatbot
Seq2Seq chatbot (Colab → GitHub): data loading, cleaning, tokenization &amp; padding, training Seq2Seq models with and without attention, evaluating with BLEU, and deploying a simple Flask UI.
